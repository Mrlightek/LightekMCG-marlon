#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here

#!/usr/bin/env ruby
ENV['RUBYOPT'] ||= '-W0'
load File.expand_path('../marlon/cli.rb', __dir__)

